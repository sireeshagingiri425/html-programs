function PropsExample(props){
    /* In the top props is an parameter we can take any thing but most
     of are take only"PROPS" */

     console.log(props)
    return(
      <div>
            <h1>
                Iam from PROPS
            </h1>
        </div>
    )
}
export default PropsExample;